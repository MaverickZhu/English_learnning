移动端（React Native 预览）

该目录提供基础页面与流程串联的 UI 示例，便于后续接入后端 API。

建议开发方式（Expo）
1. 在本目录初始化 Expo 项目
2. 替换 App.tsx 为当前文件内容
3. 运行 Expo 启动移动端

说明
- 当前仅为流程与布局示例
- 后续可接入 /auth, /practice, /exam, /reports 等接口
- 学习页已接入图片/音频访问与媒体链接绑定
- 如需本地选择并上传媒体，请安装:
  - expo-image-picker
  - expo-document-picker
